{
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint .",
    "test": "vitest run",
    "prestart": "npm run format && npm run lint && npm run test",
    "start": "node src/sum.js"
  },
  "devDependencies": {
    "eslint": "^9",
    "eslint-config-prettier": "^9",
    "prettier": "^3",
    "vitest": "^2"
  }
}
